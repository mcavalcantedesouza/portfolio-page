# NPM Configuration for Portfolio Project

# Set audit reporting level to account for transitive dependencies
# that cannot be directly fixed without breaking changes
audit-level=high

# Use production integrity checking
engine-strict=false

# Enable legacy peer dependencies resolution for compatibility
legacy-peer-deps=false
